# Emergency plan: make the QR/player flow safe first

## Immediate priority
Stop working on `/dev` and focus only on the player-facing QR path: `/host` QR → `/join?code=ROOM` → `/play`.

## What I already verified
- `https://droptrivia.app/host` returns a host page in a clean browser.
- `https://droptrivia.app/join` loads in a clean browser.
- `https://droptrivia.app/join?code=TEST` loads in a clean browser.
- `https://droptrivia.app/play` redirects to `/join` if no player session exists, which is expected.

Your screenshot still shows the generic “This page didn’t load” inside the Lovable preview, so I will treat the preview/player route as unstable until proven otherwise.

## Fix steps

1. **Reproduce the QR path from the actual host page**
   - Open `/host` in a clean browser.
   - Read the exact QR join URL generated by the host page.
   - Open that QR URL directly.
   - Fill a test nickname and submit.
   - Verify it reaches `/play` instead of the error page.

2. **Patch the player-facing crash guard**
   - Add defensive error handling around the join/play flow where browser-only session/localStorage, audio startup, avatar upload, or server-function failures can currently crash the whole route.
   - If a non-critical feature fails, the player should still see the join form or play screen, not the app-wide “This page didn’t load.”

3. **Keep `/dev` isolated**
   - Do not route players through `/dev`.
   - Do not make `/dev` changes part of the player QR path.
   - If needed, leave `/dev` broken temporarily rather than risking the customer-facing game.

4. **Verify the customer flow only**
   - Host creates room.
   - QR URL opens join form with code prefilled.
   - Test player joins.
   - Player reaches `/play`.
   - Host shows the player count increase.

5. **Publish only after QR path passes**
   - Run the security pre-publish check.
   - Publish the fixed build.
   - Re-test the published custom domain QR path.

## Success condition
A real player scanning the QR code can join from a phone and land in `/play` without seeing “This page didn’t load.”